model Company {
  id        Int       @id @default(autoincrement())
  name      String
  description String?
  phone     String?
  address   String?
  purchases Purchase[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@map("companies")
}
